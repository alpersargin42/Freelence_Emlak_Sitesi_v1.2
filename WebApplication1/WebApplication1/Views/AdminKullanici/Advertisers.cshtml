@using EntityLayer.Concrate
@model List<Advertiser>
@{
    ViewData["Title"] = "Advertisers";
    Layout = "~/Views/Shared/Admin_Layout.cshtml";
}

@* <!-- SweetAlert CSS dosyasını projenize ekleyin -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
 *@
@* <script>
    function confirmDelete(advertiserid) {
        Swal.fire({
            title: 'Emin misiniz?',
            text: "Bu ilanı silmek istediğinize emin misiniz?",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Evet, Sil',
            cancelButtonText: 'İptal'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = "/AdminKullanici/DeleteAdvertiser/" + advertiserid;
            }
        });
    }
</script> *@

<h1>Kullanıcılar</h1>
<br/>
<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Kullanıcı Adı</th>
        <th>Kullanıcı Maili</th>
        <th>Kullanıcı Şifresi</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <th>@item.AdvertiserID</th>
            <td>@item.AdvertiserName</td>
            <td>@item.AdvertiserMail</td>
            <td>@item.AdvertiserPassword</td>
            @* <td><button onclick="confirmDelete(@item.AdvertiserID)" class="btn btn-gradient-danger">Sil</button></td> *@
        </tr>
    }
</table>


